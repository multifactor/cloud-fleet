# Exact strings that may appear verbatim even though they contain a denied token.
# Keep this list tiny and justify every line: each entry is a hole in the gate.
#
# The repository's own address. You cannot document installing the plugin without naming the GitHub
# org that hosts it; the org name is not a product name and appears nowhere else.
multifactor/cloud-fleet
github.com/multifactor/cloud-fleet
https://github.com/multifactor/cloud-fleet
# Placeholder tool identifiers used by the spec and the adapter template to show the SHAPE of a
# tool name. They name no real server.
mcp__example__
mcp__example_tracker__list_teams
